{#<form method="post" action="{{ path('admin_category_delete_image', {'id': category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this image?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
    <button class="btn"><i class="fa fa-trash imgDelete"></i></button>
</form>#}
<a class="btn btn-primary" href="{{ path('admin_blog_post_delete_image', {'blog_post_id':blog_post.id,'image_id': image.id}) }}" title="Удалить изображение"><i class="fa fa-trash imgDelete"></i></a>